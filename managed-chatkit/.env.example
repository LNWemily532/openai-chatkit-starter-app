# Environment Variables Template
# 
# IMPORTANT SECURITY NOTES:
# - NEVER commit this file with real values
# - Copy this file to .env.local and fill in your actual values
# - .env.local is already in .gitignore and will NOT be committed
# - Keep your API keys secret at all times
#
# To use: Copy this file to .env.local and replace the placeholder values

# OpenAI API Key (required)
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# ChatKit Workflow ID (required)
# Get this from OpenAI Agent Builder (starts with wf_...)
VITE_CHATKIT_WORKFLOW_ID=wf-your-workflow-id-here

# ChatKit API Base URL (optional, defaults to https://api.openai.com)
CHATKIT_API_BASE=https://api.openai.com
VITE_CHATKIT_API_BASE=https://api.openai.com

# Environment (set to 'production' for production deployment)
ENVIRONMENT=development
NODE_ENV=development

# CORS Origins (optional, comma-separated for production)
# In production, set this to your domain: https://sop-chatbot.livelihoodnw.org
# Leave empty to use default production settings
CORS_ORIGINS=
